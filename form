<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Spam Detection App</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background-color: #f8f9fa;
            padding-top: 50px;
        }
        .card {
            max-width: 600px;
            margin: auto;
            padding: 30px;
            border-radius: 20px;
            box-shadow: 0 8px 20px rgba(0, 0, 0, 0.1);
        }
        .btn-check {
            background-color: #0d6efd;
            color: white;
            font-weight: bold;
        }
        .result {
            font-size: 1.2rem;
            font-weight: bold;
        }
    </style>
</head>
<body>

<div class="container">
    <div class="card">
        <h2 class="text-center mb-4">📩 Spam Detection App</h2>
        <form action="/predict" method="post">
            <div class="mb-3">
                <label for="message" class="form-label">Enter your message:</label>
                <textarea class="form-control" id="message" name="message" rows="4" placeholder="Type here..." required></textarea>
            </div>
            <div class="d-grid">
                <button type="submit" class="btn btn-check">Check Spam 🔍</button>
            </div>
        </form>

        {% if prediction %}
            <div class="mt-4 alert alert-{{ 'danger' if 'SPAM' in prediction else 'success' }}">
                <span class="result">{{ prediction }}</span>
            </div>
        {% endif %}
    </div>
</div>

</body>
</html>
